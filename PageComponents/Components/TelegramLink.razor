<TextLink href="@($"https://t.me/{Username}")" target="_blank">
    @if (ChildContent is null)
    {
        @(string.IsNullOrEmpty(DisplayName) ? Username : DisplayName)
    }
    else
    {
        @ChildContent
    }
</TextLink>


